[pytest]
asyncio_mode = strict
markers =
    integration: marks tests that require a running database/external service (deselect with -m "not integration")
    live: marks tests that make real API calls (e.g. Anthropic) — skip unless ANTHROPIC_API_KEY is set
