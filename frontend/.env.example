# ── Backend API ──────────────────────────────────────────────────────────────
# Set this to your deployed backend URL (e.g. Railway, Render, Fly.io).
# Leave empty to use the Next.js rewrites proxy (requires same-domain deployment).
NEXT_PUBLIC_API_URL=https://your-backend.railway.app
